<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.medical.polyp.mapper.PolypModelMapper">

    <resultMap id="PolypModelResult" type="PolypModel">
        <result property="modelId" column="model_id"/>
        <result property="modelName" column="model_name"/>
        <result property="modelVersion" column="model_version"/>
        <result property="modelPath" column="model_path"/>
        <result property="status" column="status"/>
        <result property="isDefault" column="is_default"/>
        <result property="confThreshold" column="conf_threshold"/>
        <result property="remark" column="remark"/>
        <result property="createBy" column="create_by"/>
        <result property="createTime" column="create_time"/>
        <result property="updateBy" column="update_by"/>
        <result property="updateTime" column="update_time"/>
    </resultMap>

    <select id="selectPolypModelByModelId" parameterType="Long" resultMap="PolypModelResult">
        select model_id, model_name, model_version, model_path, status, is_default, conf_threshold,
               remark, create_by, create_time, update_by, update_time
        from polyp_model
        where model_id = #{modelId}
    </select>

    <select id="selectCurrentPolypModel" resultMap="PolypModelResult">
        select model_id, model_name, model_version, model_path, status, is_default, conf_threshold,
               remark, create_by, create_time, update_by, update_time
        from polyp_model
        where status = 'ACTIVE' and is_default = 1
        limit 1
    </select>

</mapper>
